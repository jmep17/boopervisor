import { PageHeader } from "@/components/page-header";

export default function HistoryPage() {
  return <PageHeader title="History" description="Every change Boopervisor has written, with a diff and a way back." />;
}
