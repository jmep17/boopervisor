import { PageHeader } from "@/components/page-header";

export default function McpPage() {
  return <PageHeader title="MCP servers" description="The MCP servers Claude Code will load, per scope, and their state." />;
}
