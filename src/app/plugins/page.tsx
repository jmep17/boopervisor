import { PageHeader } from "@/components/page-header";

export default function PluginsPage() {
  return <PageHeader title="Plugins" description="Installed plugins, the marketplace each came from, and its state." />;
}
