import { PageHeader } from "@/components/page-header";

export default function SkillsPage() {
  return <PageHeader title="Skills" description="The skills available in the selected scope, and whether each is enabled, disabled or archived." />;
}
